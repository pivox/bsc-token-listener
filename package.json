{
  "name": "bsc-token-listener-bot",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/app.ts",
    "start": "node dist/src/app.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --import ./tests/setup-env.ts --test tests/*.test.ts",
    "test:chain": "tsx --import ./tests/setup-env.ts --test tests/safety-probe-local.test.ts",
    "test:testnet": "tsx scripts/check-testnet-safety-probe.ts",
    "test:postgres": "tsx --test tests/postgres/*.test.ts",
    "rpc:check": "tsx scripts/check-rpc.ts",
    "pair:diagnose": "tsx scripts/diagnose-pair-created.ts",
    "db:migrate": "tsx scripts/migrate.ts",
    "db:reset-runtime": "tsx scripts/reset-runtime.ts",
    "deploy:probe": "tsx scripts/deploy-safety-probe.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.22.0",
    "pino": "^10.3.1",
    "viem": "^2.55.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.0",
    "hardhat": "^3.12.0",
    "solc": "^0.8.36",
    "tsx": "^4.23.1",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=22"
  }
}
